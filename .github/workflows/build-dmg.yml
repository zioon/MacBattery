name: Build and release DMG

# Trigger on version tags (v*) and allow manual dispatch.
"on":
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    # macOS-14 (arm64) hosted runner is free on public repos.
    # Build a universal binary (arm64 + x86_64) so it runs on both
    # Apple Silicon and Intel Macs.
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build (release, universal)
        run: swift build -c release --arch arm64 --arch x86_64
        timeout-minutes: 30

      - name: Assemble .app bundle
        run: |
          set -e
          APP="dist/MacBattery.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          # Universal (--arch) builds land under .build/apple/Products/Release;
          # single-arch builds land under .build/release. Locate the binary either way.
          BIN="$(find .build -path '*Release/MacBattery' -type f 2>/dev/null | head -n1)"
          [ -n "$BIN" ] || BIN=".build/release/MacBattery"
          echo "using binary: $BIN"
          cp "$BIN" "$APP/Contents/MacOS/MacBattery"
          echo '' | base64 --decode > "$APP/Contents/Info.plist"

          # 发版时用 tag（如 v1.2.0）覆盖 Info.plist 版本号，保证应用内自动更新能正确比对版本。
          if [ -n "$GITHUB_REF_NAME" ] && [ "${GITHUB_REF_NAME#v}" != "$GITHUB_REF_NAME" ]; then
            VER="${GITHUB_REF_NAME#v}"
            /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $VER" "$APP/Contents/Info.plist"
            /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $VER" "$APP/Contents/Info.plist"
            echo "set bundle version to $VER"
          fi

          codesign --force --sign - "$APP" >/dev/null 2>&1 || true

          # 把 root helper 二进制与安装脚本一并放入 dist，随 DMG 分发
          HELPER_BIN="$(find .build -path '*Release/MacBatteryHelper' -type f | head -n1)"
          if [ -n "$HELPER_BIN" ]; then cp "$HELPER_BIN" dist/macbattery-helper; fi
          cp Scripts/install_helper.sh dist/install_helper.sh 2>/dev/null || true

      - name: Create DMG
        run: |
          rm -f MacBattery.dmg
          hdiutil create \
            -volname "MacBattery" \
            -srcfolder dist \
            -ov \
            -format UDZO \
            MacBattery.dmg

      - name: Publish Release
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            MacBattery.dmg \
            --repo "$GITHUB_REPOSITORY" \
            --title "MacBattery $GITHUB_REF_NAME" \
            --generate-notes

      - name: Upload DMG (workflow_dispatch)
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: actions/upload-artifact@v4
        with:
          name: MacBattery.dmg
          path: MacBattery.dmg