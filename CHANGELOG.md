# Changelog

本文件记录 MacBattery 的重要变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.2.3] - 2026-09-24

### 修复

- **「本机不支持充电抑制」分不清成因**（v1.2.2 已发版，来自用户实际反馈）：回执里
  `supported == false` 有三种**完全不同**的来源，原先三者在界面上共用同一句提示，
  于是用户拿到的结论是自己无法处理的。现在：
  - helper 把候选 SMC 键的**只读探测结果**（是否存在 / dataSize / 原始取值）一并写进回执，
    界面在功能不可用时直接显示这一行（可选中复制），日志里也带同一份；
  - 成因拆成三条：**键一个都不存在**（机型没有该能力）／**键存在但取值不在已知范围**
    （缺一个取值映射，可救）／**SMC 打不开**（`smc_open_failed`，环境问题，不是机型不支持）；
  - 「由回执内容推导成因」做成纯函数 `ChargeLimitWire.unavailableReason(for:)` 并加单测 ——
    这段映射决定用户看到哪句提示，放在 App 层就永远跑不到测试（单测只依赖 MacBatteryCore）；
  - 额外只读探测一组候选键（`CHTE` / `CH0I` / `CH0J` / `CH0K` / `ACEN` / `CHWA` / `BCLM`）：
    **只读不写**，语义未经确认的键绝不参与执行，只用于回答「这台机器到底有哪些相关键」；
  - 回执新增可选字段 `probed`，旧版 helper 不带它也能正常解析（不会退化成「没装 helper」）。
  - **不再靠猜键名**：helper 会枚举 SMC 键命名空间（读特殊键 `#KEY` 拿键总数 + 按索引取键名），
    把前缀为 `CH` / `BC` / `BF` / `AC` 的键名一并**只读探测**后上报（去重、限量 24 条）。
    候选键名是我根据公开实现列的猜测，机型之间并不统一 —— 猜错就永远卡在「没找到可用的键」，
    而每猜一轮都要用户重装一次 helper；枚举拿到的是**这台机器真实拥有哪些键**。
    读不到 `#KEY` 只影响这份诊断、不影响其它功能；字节序存疑时用「按名读一次」自校验
    （顺序反了会得到 `B0HC` 这种**同样是可打印 ASCII** 的名字，光看字符发现不了）。
- **提示文案改准 + 显示 helper 当前状态**（同一反馈的跟进）：实测机器为 `MacBookAir8,1`，
  回执是 `no_charge_key` 且 `systemPower` 有正常读数 —— 即 **helper 在跑、SMC 可读，只是没找到可用的键**。
  原先那句「本机不支持充电抑制」把"没人干活"和"干不了这个活"混成了同一句话，已改为：
  - 状态行只说**对功能的影响**（「未找到可用的充电抑制键（CH0B / CH0C）：本机无法用这种方式断开充电」），
    不再下"本机不支持"这种绝对结论 —— 因为"键不存在"与"键在但取值不认识"是两回事；
  - 新增**独立的 helper 状态行**（`root helper：运行中（回执 2 秒前）· SMC 可读`，
    或未安装 / 未在运行 / 版本过旧），功能关着时也显示 —— 它是这个区块的前置依赖；
    回执年龄让"运行中"变成可核对的事实，而不是一句无法验证的断言。

## [1.2.2] - 2026-09-24

### 新增

- 充电上限：可设定一个充电上限（50%–100%，步长 5%，默认 80%，100% 表示不限制），接电时充到上限即暂停，
  电量回落到「上限 − 5%」再恢复（迟滞，避免阈值附近反复启停）。
  - 设置面板提供开关、上限滑块（右侧显示当前上限）与**实际生效状态**（充电中 / 已到上限 / 未接电源 /
    未装 helper / helper 未在运行 / helper 版本过旧 / 本机不支持）；菜单栏新增「充电上限（80%）」勾选项。
  - 挂件在充电环上画一条**上限刻度线**（位置即上限），到达上限后行首图标变 `bolt.slash`、
    显示「限充中」与上限值。
  - **执行者仍是 root helper**：macOS 无公开 API 可停止充电，实际手段是写 AppleSMC 的充电抑制键
    （`CH0B` / `CH0C`），因此复用既有的 `install_helper.sh`；**未安装 helper 时功能不生效**，
    界面明确写出原因而不假装成功。**升级后需重新运行一次 `install_helper.sh`**
    （新增了指令/回执协议，版本不符时界面会提示重装）。
  - 安全与故障安全：helper 只接受白名单动作 / 键 / 取值（`/tmp` 可被任意用户伪造指令文件），
    只写 `dataSize == 1` 的键，写完**读回校验**，读到不认识的取值时跳过该键；
    App 每 5 秒刷新指令，超过 30 秒未刷新即视为 App 已退出，helper 复位为「允许充电」——
    不会出现「关掉挂件后电永远充不满」。
  - 决策（阈值 / 迟滞 / 触发条件）抽到 `MacBatteryCore/ChargeLimit.swift`（纯逻辑），
    与 App↔helper 的协议定义 `ChargeLimitWire.swift` 一起新增单测覆盖 —— 本机没有 Swift 工具链，
    这是唯一能在发版前验证「到 80% 到底会不会停」的地方。
- DMG 内加入指向 `/Applications` 的「应用程序」快捷方式：挂载后可直接把 `MacBattery.app`
  拖进快捷方式完成安装，不必再另开一个访达窗口找「应用程序」。
  打包链路同时从「`hdiutil create -srcfolder` 一步成盘」改为「先出可读写镜像 → 挂载后写入快捷方式 →
  再转压缩只读 DMG」，并新增一条门禁：**挂载成品 DMG 校验快捷方式存在且确实指向 `/Applications`**，
  同时复核 `.app`（含可执行文件）与 `macbattery-helper` 的落位没被新的打包路径带偏。
- 挂件可隐藏：设置窗口新增「显示挂件」开关（**默认开启**，老用户升级后行为不变），
  菜单栏同步提供「显示挂件」勾选项。隐藏只摘掉浮窗本身（`orderOut`，不销毁视图树），
  采样、日志与充电上限等后台功能照常运行；隐藏后菜单栏图标仍在，从那里或设置里即可恢复。
  挂件隐藏时，设置里的「大小 / 位置 / 鼠标穿透」置灰（无可作用对象）。

### 变更

- 占位符检查修正（这份检查有两处实现，必须同步改）：`Scripts/check_localization_keys.py` 与
  `Tests/MacBatteryCoreTests/LocalizationTests.swift` 的占位符扫描，原先都会把转义后的字面百分号
  `%%` 误当成占位符（英文 `"Reached the %d%% limit"` 被解析出 `% l`），使译文被误判为「占位符漂移」、
  只能改措辞去绕开检查。现在两处都先剥掉 `%%` 再扫描。
- 挂件使用电池时的那行去掉「已用」前缀文字，只显示 `H:MM` 时长本身（如 `0:45`）：
  该行与下一行的「剩余 1:23」靠行首图标区分，不再用文字占宽。
  随之移除已无调用点的文案键 `hud.used_prefix`（`zh-Hans` / `en` 两份资源同步删除，避免留下死键）。

## [1.2.1] - 2026-09-22

### 修复

- **启动即崩溃（v1.2.0 引入）**：定位语言资源时的第一个候选是 SwiftPM 的 `Bundle.module`，
  而该访问器在找不到资源 bundle 时走的是 `fatalError` —— 资源布局一旦与预期不符，应用就会在
  `applicationDidFinishLaunching` 阶段直接 SIGILL（`EXC_BAD_INSTRUCTION`），表现为「一打开就闪退」。
  现改为**纯查询式**搜索（`.app` 的 `Contents/Resources` → 可执行文件所在目录 → SwiftPM 资源 bundle），
  任何一环缺失都只是返回 `nil` 并回退到默认语言、再回退到键名，**绝不让「找不到翻译」升级成「应用起不来」**。
- 打包时多语言资源改为**双份落位**：`Contents/Resources/<lang>.lproj`（macOS 标准布局，同时让
  `CFBundleLocalizations` 真正生效）+ SwiftPM 资源 bundle（`swift run` 用）。原先只放了后者，
  而 `Bundle.module` 的查找位置与实际布局不一致。

### 其他

- CI 新增**启动冒烟测试**：`.app` 组装完成后直接运行二进制，8 秒后进程仍在才算通过。
  上一版的门禁只断言「资源文件被拷进了 `.app`」，没有验证「App 真能起来」，因此把这个崩溃放行了。

## [1.2.0] - 2026-09-22

### 新增

- 多语言（国际化）支持：界面文案、提示语与错误信息统一抽取到语言资源文件（`Sources/MacBatteryCore/Resources/<lang>.lproj/Localizable.strings`），
  首批提供简体中文（默认语言）与英文，可在「设置」顶部切换并**即时生效、无需重启**；首次运行按系统首选语言自动匹配，
  匹配不到时回到简体中文。语言选择持久化在 `UserDefaults`，不写入 `AppleLanguages`、不改系统偏好。
- 翻译缺失回退：所选语言缺某条文案时回退到默认语言（简体中文），两处都缺时显示键名并记入日志 —— 不崩溃、不显示空白，
  后续新增文案只需补键，漏翻不会导致界面出问题。
- 复数与区域化格式：样本数与时长按语言区分单复数（英文 `1 sample` / `2 samples`）；数字改按区域格式化（原先 `String(format: "%.1f")` 在德语等区域小数点是错的）；
  三处写死的日期格式改由系统按区域解析模板，英文等 12 小时制区域的图表时间轴不再显示成 24 小时制。
- 单测新增多语言护栏：三级回退链、系统语言匹配、复数类别判定、区域化数字与日期，以及两份资源文件的键名与格式占位符一致性
  （漏翻译、占位符漂移直接让 CI 失败）。
- CI 新增「硬编码文案」守护（`Scripts/check_hardcoded_strings.py`）：界面层残留含中文的字符串字面量会让 CI 失败 ——
  `Text("中文")` 会被当成 `LocalizedStringKey` 走 `Bundle.main`，绕过应用内的语言选择，表现为「切了语言这一处不变」，肉眼极难发现。
- CI 新增「语言资源一致性」守护（`Scripts/check_localization_keys.py`）：各语言键名一致（漏翻译即失败）、格式占位符数量一致、
  代码里引用的键确实存在（`L("拼错的键")` 运行时会在界面上显示键名），以及 `.one` / `.other` 必须成对。
- CI 打包补上多语言资源：`MacBattery_MacBatteryCore.bundle` 随 `.app` 进 `Contents/Resources/` 并断言 `zh-Hans.lproj` / `en.lproj` 存在
  （资源缺失时 `Bundle.module` 取不到，界面文案会整体回退成键名）；`Info.plist` 补 `CFBundleDevelopmentRegion` 与 `CFBundleLocalizations`。

### 变更

- 挂件时长显示统一为 `H:MM`：使用电池时的「已用时长」与「预计剩余时间」不再区分是否满 1 小时，
  一律输出 `H:MM`（如 `0:45` / `1:23`），消除原本「45 分 → 1:23」在跨过整点时的形状跳变，观感与系统菜单栏一致。

## [1.1.14] - 2026-09-22

### 变更

- 使用电池供电时，挂件底部的「充电功率 / 电压·电流」改为「已用时长 / 预计剩余时间」：
  剩余时间采用与系统菜单栏同源的 IOPS 官方估算（系统仍在估算时显示「估算中…」）；
  已用时长从开始放电的时刻起算，插回电源即切回充电功率显示。
  接通电源但已充满（非充电）时维持原占位，不算作使用电池。

## [1.1.13] - 2026-09-16

### 修复

- 历史图表的磁盘回填会覆盖刚启动时采到的新数据：功率日志的回填原先直接用文件历史整体替换内存 —— 同一问题 v1.1.9 只在电池健康日志修过、功率日志漏修。现抽出按时间合并去重的共用实现，两个日志共用；「同时间戳由新采样覆盖历史」也从依赖排序实现细节变为显式保证。
- 历史 CSV 的读取失败（磁盘故障、文件损坏）与「文件过大被截断」此前混在同一条提示里，写入侧多个失败点被静默吞掉。现在创建 / 打开 / 定位 / 读取 / 删除失败都会记录到系统日志，截断单独提示。
- 退出应用时最后一段待落盘样本可能丢失：清理逻辑原先放在 `deinit` 里经 `Task { [weak self] }` 执行，而析构时 `weak self` 必然为 nil —— 该路径从未真正运行过。现改为显式 `shutdown()`，由 `applicationWillTerminate` 调用，退出时真正停止采样并完成最后一次落盘。

### 性能

- 拖动「整机功率估算上限（TDP）」滑块不再重建整棵挂件视图树：原先每次设置变更都重建，而 TDP / 穿透开关根本不参与挂件视图的构造。视图树只在尺寸档位变化时重建。
- 图表绘制层抽取为共享模块：历史图表与电池健康图表原先各写一份几乎相同的绘图区几何、刻度与格式化、悬浮浮层、滚轮 / 拖拽交互与折线描边实现，现统一到 `Charting/` 单一来源，图表类修复只改一处、两个图表行为天然一致。

### 其他

- 新增纯逻辑层 `MacBatteryCore` 与单元测试（版本比较、日志合并、功率估算、挂件定位几何矩阵），CI 在构建与发版前先跑 `swift test`，并守护该层不引入 AppKit / IOKit / SwiftUI 依赖。
- CI：universal 构建改为分架构构建后用 `lipo` 合成 —— 引入 Swift 库 target 后，单条命令的跨架构构建会触发 Swift 5.10 编译器崩溃；`workflow_dispatch` 输入改经环境变量中转；checkout 完整拉取。
- 菜单栏与设置窗口从浮窗控制器拆分为独立组件；删除两处无调用点的死代码。

## [1.1.12] - 2026-09-16

### 修复

- 主线程彻底不再做硬件读取：功率采样的触发路径改为**入队**（原先「启动首拍 / 每次插拔电源」会在主线程就地执行完整采样，包括拉起 `pmset` 子进程），电池健康的 IOKit 读取也移到专用后台队列（原先每 60 秒在主线程读一次注册表，与 0.5s 功率采样长期重叠）。并在两个硬件读取入口加上运行期断言 —— `dispatchPrecondition` 基于 `precondition`，**发布版同样生效**，未来任何人新增主线程硬件读取路径都会立刻暴露而不是悄悄拖慢界面。
- 电池健康「重置数据」后在途采样可能回潮：健康采样改为异步后出现一个窄窗口 —— 采样已进入后台队列、读取完成、尚未回到主线程时点了「重置数据」，那个陈旧点会被追加回内存并在清空磁盘之后再次落盘（表现为清空后曲线立刻回潮一个点）。现在在采样发起时快照代际、回到主线程时校验丢弃。
- 历史图表回填历史会覆盖刚启动的新采样：`PowerLogger` 的磁盘回填原先直接用文件历史整体替换内存 —— 同一问题 v1.1.9 只在电池健康日志修过、功率日志漏修。现抽出按时间合并去重的共用工具，两个日志共用；顺带消除「分块解码失败导致整段历史读不回来」的隐患（改用容错解码，非法字节只影响所在行）。
- 历史 CSV 写入失败被完全静默：磁盘满 / 无写权限时历史数据会无声消失且无从排查。现在创建文件、打开句柄、关闭句柄、删除文件、回填被截断等 IO 失败都会记录到系统日志。
- 退出时最后一段样本丢失：清理逻辑原先放在 `deinit` 里用 `Task { [weak self] }` 执行，而析构时 `weak self` 必然为 nil —— 这条路径从未真正跑过。现改为显式 `shutdown()`，由 `applicationWillTerminate` 调用，退出时会真正停止采样并完成最后一次落盘。

### 变更

- 电池健康窗口对照历史图表窗口逐一调整：头部改为与历史一致的图例样式（去掉与窗口标题重复的文字）；底部左侧改为「样本 N 个 · 查看最近 X」；底部右侧补上「左轴：mAh · 右轴：%」轴说明；「全部」在未命中预设时高亮；窗口内边距与最小尺寸与历史窗口一致。
- 删除「实时容量」指标（头部芯片、悬浮数值、5 秒刷新定时器与 CSV 记录列一并移除）。它对旧样本缺失电量数据时按 0 绘制，曾造成曲线从 0 突跳到当前容量的假象。
- 两个窗口的图例都显示各系列当前值，并随采样同步刷新；电池健康窗口可见期间采样加密到 5 秒（常驻仍为 60 秒），查看时数值更跟手，关闭后自动回到低速。落盘仍按「值变化才写」的门控，不会因此多写历史记录。
- TDP 估算上限的跨线程缓存加锁（原先是无同步的裸变量）。

### 其他

- CI：`workflow_dispatch` 增加版本号输入，并新增一致性校验（`AppVersion.fallback` 必须与发布版本一致、CHANGELOG 必须有对应小节）；Info.plist 版本写入改为无条件 —— 手动触发的构建不再停在硬编码的 `1.0.0`。
- CI：workflow 输入改经环境变量中转，不再直接插入 shell；checkout 改为完整拉取，使文档描述的版本回退真正可用。

## [1.1.11] - 2026-09-15

### 修复

- 主线程并发访问硬件缓存可能导致读数错乱或崩溃：采样此前依赖「静态缓存只被后台串行队列访问」这一**只存在于注释里的约定**，但主线程实际存在 5 条硬件访问入口（启动首拍、每次插拔电源、健康日志启动采样、**每 60 秒**的周期采样、打开健康窗口）。休眠唤醒后 `AppleSmartBattery` 句柄失效并走 `rebuildService()` 时，其 `IOObjectRelease` 可能与另一线程的读取重叠，造成 use-after-free —— 表现为数字突然变 `0` / `--`，或应用闪退。现为 `BatteryReader` / `SMCReader` / `SystemPower` 的静态缓存各自加锁，并约束「锁绝不跨 `pmset` 子进程持有、锁之间不嵌套」，避免把子进程阻塞换成锁阻塞。
- `pmset` 读取管道顺序倒置且无超时：`waitUntilExit()` 原先在读取管道**之前**调用，输出超过管道缓冲时父子进程互等；子进程挂起还会让采样线程永久卡死。现改为**先读管道再等退出**，并新增 1.5 秒看门狗（超时终止子进程）。
- 选「位置 → 四角」之后角落锚定丢失：程序化定位会触发 `NSWindowDidMoveNotification`，被监听器误判为用户拖拽，把预设角改写成「自定义位置」，导致此后改尺寸 / 换分辨率不再贴角。现以程序化原点做值过滤（命中即忽略，并保留标记以正确处理一轮内发生的多次程序化定位）。

### 变更

- `pmset -g batt` 结果缓存由 2 秒延长到 5 秒，进程 fork 频率由约 30 次/分降到约 12 次/分 —— 一个监测电池的工具不应为了读电池状态而持续消耗电池。
- 面板定位由「先 `setContentSize`、再 `setFrameOrigin`」两步改为**单次 `setFrame`**，消除「尺寸已变、原点未随」的中间态（角落锚定的挂件会瞬间偏离角落）。
- 整机功率为**估算值**时，挂件上的数字以 `~` 前缀标注，设置面板同步说明。此前估算值与实测值在界面上无任何区分，用户会把估算当成实测。
- SMC 整机功耗候选键提升为 `Sources/SMCBridge/SMC.c` 中的**唯一定义处**（此前主程序、helper、README 三处各写一份，且 README 已写错）。

### 其他

- root helper 主循环补 `autoreleasepool`：该进程以 launchd 守护常驻、几乎不重启，原先每轮经 Foundation 产生的自动释放对象无处回收，内存单调增长。
- README：修正残留的 Windows 路径、候选键列表（`EDR0` → `PSYS`）、「读不到显示 `--`」的过时描述；补充 ad-hoc 签名下的 Gatekeeper 首次打开说明。

## [1.1.10] - 2026-09-15

### 新增

- 电池健康新增「实时容量」指标：当前最大容量 × 当前电量百分比，随电量变化实时刷新，在顶部指标条单独展示（独立配色）。采样记录新增 `levelPercent` 字段，CSV 增加第 6 列，并兼容旧 5 列文件的回填（缺列记为未知）。

### 修复

- 快充满的涓流充电被截断、误判为不在充电：`pmset -g batt` 在快充满时输出 `finishing charge`（收尾涓流仍在充，菜单栏仍显示正在充电），但其不含独立词 `charging`，现将其计入充电；纯 `charged`（真正充满）仍不算充电。
- 拔/插电后约 10 秒才感知状态切换：电源事件触发后短窗口内改用 IOPS 即时状态（与系统菜单栏电池图标同步、无子进程/无缓存，插拔立即反映）并让 pmset 缓存失效，断电后状态在下一个采样拍（≤0.5s）即切换。

## [1.1.9] - 2026-09-15

### 修复

- 电池健康图表重启后曲线为空：回填磁盘历史与启动时同步写入内存的新采样存在竞态——`backfillHistory()` 异步读取磁盘的同时，`sample(force: true)` 已先把启动瞬间的点写入内存，导致异步回填的回调因内存缓冲非空而直接丢弃磁盘历史，每次启动都只剩启动瞬间那 1 个点、健康曲线始终空白。改为回填时把磁盘历史与内存新采样按时间合并、去重并裁剪到容量上限，历史得以持续积累并正确显示。

## [1.1.8] - 2026-09-15

### 新增

- 充电面板在充电功率下方新增电压 · 电流小字（如 `12.3V · 1.5A`），充电中实时显示。

### 修复

- 断电（放电）后仍显示充电功率：充电功率仅在正在充电时返回（`pmset -g batt` 判定为充电，或电流方向为负即物理充入），放电时返回 0，避免放电电流读数被误认为充电；电压 · 电流小字同样仅充电时显示。

### 变更

- 电池健康窗口底部工具条新增「样本 N 个 · 已记录 X」信息（与历史图表一致），并加大图表底部留白，信息不再与窗口右轴重叠。
- 电池健康图表重构：设计容量与当前最大容量（左轴，mAh）与电池健康度（右轴副坐标，%）合并为一张合成图；循环次数保留为下方单独一行子图。

## [1.1.7] - 2026-09-15

### 新增

- 历史图表与电池健康图表新增「重置数据」按钮：点击确认后清空该图表全部历史数据（内存与磁盘 CSV），从零重新积累；同时会丢弃重置前已发出的异步磁盘回填，避免旧历史被塞回内存。

### 修复

- 功率 / 电池健康日志落盘覆盖：CSV 以 `FileHandle(forWritingTo:)` 打开时文件指针位于开头，应用每次重启后新数据会从文件头部覆盖旧历史并重复写入表头，导致历史记录丢失或错位（曾出现异常时间戳行）。改为打开后移动到文件末尾追加写入；文件已存在且非空时不再重复写表头。
- 充电状态判定与功率读数解耦：充电状态改用 `pmset -g batt` 作为权威判定（与系统菜单栏电池图标同源），调用失败时退回 IOPS / IsCharging 标志 / 电流方向综合判定；电压 · 电流 · 功率读数不再因充电状态误判而被清空（放电时同样返回电流读数，由 UI 图标颜色区分状态）。

### 变更

- 电池健康窗口布局调整为与历史窗口一致：顶部指标条（标题 + 各项当前值）→ 中间图表 → 底部时间窗工具条（`1d` / `1w` / `1m` / `1q` / `1y` / `全部` / `重置数据`）；移除图表上方的时间跨度栏与右侧提示文字，元素不再超出窗口。

## [1.1.6] - 2026-09-14

### 新增

- 电池健康：多次运行也可能一直不落盘——启动与打开健康窗口时改为强制记录一条基线，即使健康值未变化也留档，曲线随运行次数随时间分散、图表不再空白。
- 电池健康：新增时间范围快捷切换 `1d` / `1w` / `1m` / `1q` / `1y` / `全部`（参考历史图表布局，当前选中项高亮）。
- 电池健康：新增跨 4 行子图的鼠标数值浮层——悬停时显示贯穿全部行的竖线，并在顶部叠显该时刻的时间与 4 项指标（当前最大容量 / 设计容量 / 健康度 / 循环次数）数值。

### 变更

- 电池健康：移除 4 行子图纵轴上的上下数值刻度标记，仅保留每行彩色标题（数值改由鼠标浮层展示）。

## [1.1.5] - 2026-09-14

### 修复

- 电池健康数据长时间不落盘：启动时磁盘历史回填的异步回调可能把启动瞬间采到的点覆盖掉，导致健康图表空白。修复为「回填完成时若缓冲已有新数据则不再覆盖」；并在健康窗口打开时立即采样一次，保证当前值即刻可见并落盘。健康数据改为「值变化」或「距上次记录超过 6 小时」任一满足即留档，即使循环次数/容量未变也周期性记录基线，历史不再空洞。

### 变更

- 历史图表右轴收缩由「按时间衰减的迟滞逼近」改为「台阶式一次性跳变」，固定窗口下右轴恒定、历史曲线绝对静止。
- 历史图表分桶平均改为「绝对时间锚定桶」：窗口平移 / 实时滚动时同一历史时刻始终落入同一桶，曲线只整体平移、形状不再波动。

## [1.1.4] - 2026-09-14

### 新增

- 历史图表底部新增时间窗口快捷切换：`5m` / `30m` / `1h` / `24h` / `全部`，当前选中项高亮；切换窗口自动回到实时跟随并复位右轴自适应。

### 修复

- 部分机型电池健康信息缺失（如 `AppleSmartBattery` 的 `DesignCapacity` 为 0）时健康图表无数据：只要当前最大容量可读即返回记录；设计容量缺省时退回 `MaxCapacity` 作粗略估算，仍不可读时健康度记为 0。

### 变更

- 历史图表曲线绘制由「逐点抽稀」改为「按像素分桶平均」：CPU / 整机功耗这类 0.5s 高频抖动的数据，在长时窗口下每个像素代表一段时间窗的均值，毛刺被抹平、趋势不丢失；同时不再每帧复制整个可见样本数组，降低长时窗口（如 24h）的复制开销。

## [1.1.3] - 2026-09-14

### 新增

- 独立「电池健康」窗口：菜单栏新增「电池健康…」（快捷键 ⌘H），电池健康图表（当前最大容量 / 设计容量 / 健康度 / 循环次数）从历史图表窗口内拆分为独立窗口展示，互不干扰。
- 历史图表十字准线与数值浮层：鼠标悬停图表时显示竖向准线，并在图表上方展示该时刻各系列数值。

### 修复

- 历史图表右轴（整机功率 W / 充电功率 W / 电压 V / 电流 A）数据范围随每次采样频繁跳动：目标范围先取整到 1-2-5 刻度边界（nice 值，小噪声不改变结果），并带迟滞——数据超界立即扩张、回落后按时间缓慢收缩，短暂尖峰不再让整个纵轴来回拉扯。

### 变更

- 历史图表窗口默认尺寸调整为 700×420（原 700×600，电池健康区域移出后收窄）；多桌面（Spaces）下仅存在于当前桌面，切换桌面时正常跟随，不占据每个桌面。

## [1.1.2] - 2026-09-14

### 新增

- 电池健康信息记录与图表：历史图表窗口下方新增独立的「电池健康」区域，以共享时间轴的多行子图展示 当前最大容量 (mAh) / 设计容量 (mAh) / 电池健康度 (%) / 循环次数，默认时间跨度 1 个月。健康数据变化极慢，随应用常驻以 60s 频率采样、仅在数值变化时才落盘到 `battery_health_log.csv`，不污染原有功率日志；支持滚轮缩放时间窗、拖拽平移、Option 缩放纵轴、「适合窗口」复位。

## [1.1.1] - 2026-09-14

### 修复

- 充电功率在部分机型（Apple Silicon 的 `Amperage` 以正值表示充电）显示为 `…` / `0`：充电功率计算改为「充电状态下取电流绝对值」，并去掉歧义的 `…` 占位，恢复充电功率显示。
- 历史图表拖拽改为基于手势起点的绝对跟随（1:1 跟手、不再累计漂移）；滚轮缩放钳制单次幅度，触控板慢滚更平滑、鼠标滚轮不剧跳。
- 历史图表窗口失焦后不再自动隐藏 / 关闭，避免点击桌面或其它窗口时图表闪退。

## [1.1.0] - 2026-09-14

### 修复

- 插上充电器后约 8s 才显示「充电中」：充电状态判定从 `AppleSmartBattery` 的 `IsCharging` 固件属性改为 IOPS（`IOPSCopyPowerSourcesInfo`）官方电源源的 `kIOPSIsChargingKey`，与系统菜单栏电池图标完全同步，插拔即判、不再滞后。
- 电压 · 电流小字改为由充电状态控制显示：充电中（含涓流暂停、电流为 0.0A 的阶段）始终显示数值，未充电才显示占位，避免涓流阶段误显占位。

## [1.0.9] - 2026-09-14

### 新增

- 历史图表：菜单栏新增「历史图表…」（快捷键 ⌘G）。记录电量、充电电压 / 电流、整机功率与 CPU / 内存占用，提供实时曲线，并可回填最近一段磁盘历史。数据异步持久化到 CSV（`~/Library/Application Support/MacBattery/power_log.csv`），不阻塞界面。
- 挂件充电功率下方新增电压 · 电流小字（如 `12.3V · 1.2A`），仅在充电且有读数时显示。

### 修复

- 断开电源后约 6s 才显示「不在充电」：把采样心跳从主 RunLoop 的 `Timer` 改为后台串行队列上的 `DispatchSourceTimer`，采样不再受主线程负载与系统对低频 timer 的合并 / 节流影响；插拔电源时仍由电源事件即时补一次采样。
- 未充电却偶尔显示「充电中」：充电状态判定改为以 AppleSmartBattery 的官方 `IsCharging` 标志为权威，仅在该标志缺失的机型才退回电流方向判定，避免未充电时电流瞬时负值被误报。

### 性能

- 日志缓冲追加由「整数组复制后回写」改为「就地追加 + 手动发布」，消除每采样一次 O(n) 复制带来的主线程卡顿。

## [1.0.8] - 2026-09-14

### 性能优化

- 提升充电 / 电量数据的时效，同时显著降低整体采样开销：
  - SMC 整机功率连接只打开一次后复用，并固定读取首个有效的功率键，不再每次 `open/close` 且逐键遍历候选。
  - `AppleSmartBattery` 句柄首次匹配后缓存复用；句柄因休眠失效时自动重建。
  - 采样移到后台串行队列执行，主线程只负责 UI 更新与结果发布，SMC / IOKit 读取不再阻塞挂件动画。
  - 订阅 IOKit 电源事件，在插拔电源 / 充满 / 充电档位切换时立即补一次采样，无需等待下个定时 tick。

### 变更

- 移除充电时的流动高光与闪电图标脉动动画（此前由 30fps 角向渐变扫描与循环动画持续驱动），改为静态高亮，消除充电状态下的持续硬件 / GPU 空转以进一步省电。

## [1.0.7] - 2026-09-14

### 修复

- 流动高光在充电环路径闭合点处错位 / 脱轨：不再另画一段弧、用 `trim` 拆分绕回，而是直接用充电环自身的描边叠加角向渐变（`AngularGradient`）来绘制。高光即环的描边，天然贴环；角向渐变在 0°/360° 首尾相接，不存在路径闭合点，全程贴合环轨。

### 变更

- 取消充电环本身的呼吸晃动：移除环的呼吸光晕脉动，进度弧外发光改为静态固定半径。
- 充电图标改为周期性发光：辉光亮度 / 半径在 `0.25↔1.0`、`0.6↔3.0` 之间以 1.1s 往复脉动，仅图标亮暗变化，环保持静止。

## [1.0.6] - 2026-09-14

### 修复

- 流动高光跨过充电环路径闭合点（起点）时，`round` 线帽在两端叠加出一坨、与充电环脱轨：改为 `butt` 平头线帽，绕回两段在闭合点处平齐相接，高光全程贴合环轨。

### 变更

- 弱化充电特效幅度：移除闪电图标的脉冲放大（避免带动中间功率文字与其单位 W 一起抖动），并将呼吸光晕、进度弧外发光、闪电辉光的强度与半径整体调低，文字区保持稳定。

## [1.0.5] - 2026-09-14

### 修复

- 充电外发光被窗口边界裁剪：面板四周增加 6pt 透明余量（可见底盘仍为 58×58），并在窗口定位时扣除该余量，保证可见底盘与屏幕边缘的距离不变。
- 流动高光长度随流动不断变长：改用 `TimelineView` 由时钟直接求值（不再经过 SwiftUI 隐式动画二次插值），并以「绕回双段」画法保证高光长度恒定。

## [1.0.4] - 2026-09-14

### 新增

- 应用图标：新增 `AppIcon.png` / `AppIcon.icns` 及生成脚本 `Scripts/make_icon.py`，图形元素（背景、充电环、CPU/RAM 半环）倒角相互贴合。
- 充电视觉效果：呼吸光晕、扫过高光、闪电脉冲。
- 环色随电量平滑过渡（红 → 橙 → 黄绿 → 绿 → 青绿）。

### 变更

- CPU/RAM 内环改为上下半环布局（上半环 CPU、下半环 RAM），与充电环精确圆角贴合。

## [1.0.3] - 2026-09-14

### 新增

- 在线自动更新：启动时自动查询 GitHub Releases，发现新版本后自动下载 DMG 到「下载」文件夹，并弹窗提示安装。
- 菜单栏新增「检查更新…」，设置面板新增当前版本号与「检查更新」按钮。

### 变更

- CPU 内环改为自基点（0.5）反向生长，与 RAM 的正向生长对称。

### 构建

- 发版时 CI 用 git tag 覆盖 `Info.plist` 版本号（此前为硬编码 `1.0.0`，会导致应用内更新比对失效）。
